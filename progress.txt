## Codebase Patterns
- Use `createApp()` from `src/server/app.ts` to get a testable Express app (no `listen` call)
- `src/server/index.ts` is the thin entry point that imports `createApp()` and calls `listen()`
- MCP endpoint at POST /mcp creates a fresh server+transport per request (no session persistence across requests)
- MCP Streamable HTTP transport requires `Accept: application/json, text/event-stream` header
- MCP responses come as SSE (text/event-stream) — parse `data:` lines to extract JSON-RPC payloads
- `createMcpExpressApp()` from the SDK adds body-parsing middleware that conflicts with `express.json()` — use plain `express()` instead when manual MCP handling is in place
- Tests use Node built-in test runner (`node:test`) with `tsx` for TypeScript — run via `npm test`
- Without DATABASE_URL, the repository falls back to in-memory seed data (good for tests)
- Supabase direct connections (`db.{ref}.supabase.co`) use IPv6 — on Render, use the pooler at `aws-0-{region}.pooler.supabase.com:6543` with username `postgres.{ref}`
- Render service URL: https://agenthunt-web.onrender.com | Service ID: srv-d743cg15pdvs7380qi80

---

## 2026-03-28 - US-001
- What was implemented: Integration tests for MCP endpoint and HTTP API
- Files changed:
  - `src/server/app.ts` (new) — extracted Express app creation into importable module
  - `src/server/index.ts` — simplified to import `createApp()` and call `listen()`
  - `tests/api.test.ts` (new) — 7 integration tests covering health, services CRUD, reviews, trust signals, and MCP endpoint
- **Learnings for future iterations:**
  - The MCP SDK's `createMcpExpressApp()` adds body-parser middleware that conflicts with `express.json()`, causing "stream is not readable" errors on POST routes. Replaced with plain `express()`.
  - MCP Streamable HTTP requires `Accept: application/json, text/event-stream` header or returns 406
  - MCP responses are SSE format, not plain JSON — need to parse `data:` lines from the event stream
  - Each POST /mcp creates a fresh server+transport, so batched JSON-RPC arrays and multi-step sessions don't work in a single request
---

## 2026-03-28 - US-002
- What was implemented: Trust signal distinction in web UI — verified vs self-reported invocation counts and trust label badge
- Files changed:
  - `web/app.js` — added verifiedInvocationCount, selfReportedInvocationCount, trustLabel to all fallback service data; added these three fields to the info grid in renderDetail(); added trust label badge to detail meta area
  - `web/styles.css` — added CSS classes for trust-badge variants (verified-healthy, self-reported, watch, verified)
- **Learnings for future iterations:**
  - The fallback data in web/app.js must be kept in sync with backend seed data fields — when the backend adds new fields, the static fallback also needs them
  - Trust label badge uses `trust-${trustLabel}` CSS class pattern for color-coding different trust levels
---

## 2026-03-28 - US-003
- What was implemented: Updated README with production deployment instructions, curl examples for MCP and REST API, llms.txt reference, and removed the launch-time checklist
- Files changed:
  - `README.md` — replaced launch checklist with production URL section, usage examples (curl for MCP JSON-RPC, REST API, llms.txt), and consolidated endpoint documentation
- **Learnings for future iterations:**
  - Typecheck script is `npm run lint` (tsc --noEmit), not `npm run typecheck`
  - MCP curl examples need both `Content-Type: application/json` and `Accept: application/json, text/event-stream` headers
---

## 2026-03-28 - US-004
- What was implemented: Updated llms.txt with complete documentation of all 8 MCP tools including descriptions and input schemas
- Files changed:
  - `llms.txt` — rewrote with MCP endpoint path, all 8 tool definitions with input fields (required/optional), and REST API endpoints
- **Learnings for future iterations:**
  - All MCP tool definitions live in `src/mcp/server.ts` using `server.registerTool()` with Zod schemas
  - Tool input schemas use zod/v4 — check `.optional()` and `.describe()` for field metadata
---

## 2026-03-28 - US-005
- What was implemented: Added healthCheckPath and NODE_VERSION env var to render.yaml for successful Render deployment
- Files changed:
  - `render.yaml` — added `healthCheckPath: /api/health`, added `NODE_VERSION: "22"` env var
- **Learnings for future iterations:**
  - Render uses `NODE_VERSION` env var (not `engines.node` from package.json) to select the Node runtime version
  - `healthCheckPath` in render.yaml should point to an existing health endpoint (GET /api/health)
  - package.json `engines.node: ">=22"` was already correct and consistent with the Render config
---

## 2026-03-28 - US-006
- What was implemented: Created the final "Launch AgentHunt" commit containing all launch artifacts (README, llms.txt, render.yaml, prd.json, progress.txt)
- Files changed:
  - `prd.json` — marked US-006 as passes: true
  - `progress.txt` — appended US-006 progress entry
- **Learnings for future iterations:**
  - The launch commit is a ceremony commit — all real work was done in prior stories. It just bundles the final state.
  - All 10 tests pass, typecheck passes, build succeeds before the launch commit.
---

## 2026-03-28 - US-007
- What was implemented: Pushed ralph/agenthunt-mvp branch to GitHub remote origin
- Files changed: None (git push only)
- **Learnings for future iterations:**
  - Branch had 7 commits ahead of origin (6 feature commits + 1 initial deployable commit)
  - Push was straightforward — no force push needed since branch was only local-ahead
---

## 2026-03-28 - US-008
- What was implemented: Created PR from ralph/agenthunt-mvp to main and merged it so Render autoDeploy targets the correct branch
- Files changed: None (PR merge only)
- **Learnings for future iterations:**
  - PR #1 was already created as a draft during initial branch setup — just needed `gh pr ready` then `gh pr merge --merge`
  - render.yaml has `branch: main`, so merging to main is required for Render autoDeploy to pick up changes
  - The PR was already merged when attempting merge — always check state first with `gh pr view`
---

## 2026-03-28 - US-009
- What was implemented: Configured Render environment variables and triggered production deploy
- Actions taken:
  - Verified Render service `srv-d743cg15pdvs7380qi80` already existed (created by previous iteration)
  - Updated service branch from `ralph/agenthunt-mvp` to `main` via PATCH API
  - Added `NODE_VERSION=22` to env vars (was missing)
  - Fixed DATABASE_URL: changed from direct Supabase connection (`db.{ref}.supabase.co:5432`) to Supabase pooler (`aws-0-us-west-2.pooler.supabase.com:6543`) to resolve IPv6 connectivity issue on Render
  - Triggered deploy via Render API — status: live
  - Verified GET /api/health returns `{"status":"ok","database":{"mode":"database","databaseConnected":true}}`
- Files changed: prd.json (already updated by hook), progress.txt
- **Learnings for future iterations:**
  - Supabase direct connections (`db.{ref}.supabase.co`) resolve to IPv6 which Render free tier can't reach — use the connection pooler at `aws-0-{region}.pooler.supabase.com:6543` instead
  - Render API: PATCH /v1/services/{id} to update branch, PUT /v1/services/{id}/env-vars to set all env vars (replaces all), POST /v1/services/{id}/deploys to trigger deploy
  - Render service URL is `https://agenthunt-web.onrender.com` (slug-based, not just `agenthunt.onrender.com`)
  - Use `postgres.{ref}` as the username for Supabase pooler connections (not just `postgres`)
---

## 2026-03-28 - US-010
- What was implemented: Verified all 5 production endpoints and updated README.md to use the correct production URL (agenthunt-web.onrender.com)
- Production verification results:
  - GET /api/health → `{"status":"ok","database":{"mode":"database","databaseConnected":true}}` ✓
  - GET /api/services → returns seeded services array ✓
  - POST /mcp (JSON-RPC list_services) → returns SSE with service data ✓
  - GET /llms.txt → accessible, returns tool documentation ✓
  - GET / → 200 OK, web UI served ✓
- Files changed:
  - `README.md` — replaced all `agenthunt.onrender.com` references with `agenthunt-web.onrender.com`, removed "(update after first deploy)" note
  - `prd.json` — marked US-010 as passes: true
  - `progress.txt` — appended US-010 progress entry
- **Learnings for future iterations:**
  - Render service slugs include the service type suffix (e.g., `-web`) — always verify the actual deployed URL rather than assuming the slug matches the service name
---

## 2026-03-28 - US-011
- What was implemented: Drafted and posted Discord launch announcement to the 🎥-live-and-updates channel in the "Ralphthon by Team Attention" server
- Actions taken:
  - Opened Discord via agent-browser (session was already authenticated)
  - Identified hackathon server "Ralphthon by Team Attention" via JS query of guild nav items
  - Navigated to 🎥-live-and-updates channel (channel ID 1485922789021188176)
  - Composed and posted announcement including: product name, one-line description, production URL, MCP endpoint URL, llms.txt URL
  - Message posted by user "thefron" and verified visible in channel
- Files changed:
  - `prd.json` — marked US-011 as passes: true
  - `progress.txt` — appended US-011 progress entry
- **Learnings for future iterations:**
  - Discord's message editor uses a Slate-based contenteditable div, not a standard textbox — use `document.execCommand('insertText', false, text)` to insert text
  - `agent-browser keyboard inserttext` fails on text with special characters — use `eval` with `execCommand` instead
  - Discord server list can be queried via `document.querySelectorAll('[data-list-item-id]')` filtered by `guildsnav` prefix to get server IDs and names
  - Channel list uses `channels___` prefix on `data-list-item-id` attributes
  - Session persistence works well — no re-login needed if the browser session was previously authenticated
---

## 2026-03-28 - US-009
- What was implemented: Removed all seed data, mock fallbacks, and the seedIfEmpty() method from the backend
- Files changed:
  - `src/db/repository.ts` — removed import of services.json, removed seededServices constant, removed seedIfEmpty() method and its call from init(), changed all no-DB fallbacks from seededServices to return []/null, removed in-memory write paths from submitService/submitReview/recordVerifiedInvocation
  - `src/seed/services.json` — deleted (and removed src/seed/ directory)
  - `tests/api.test.ts` — updated to not rely on pre-existing seed data; tests 404 responses for missing services instead
  - `tests/serviceCatalog.test.ts` — rewritten to verify empty-state behavior without database (empty arrays, null trust signals)
- **Learnings for future iterations:**
  - Without DATABASE_URL, writes (submitService, submitReview, recordVerifiedInvocation) are effectively no-ops — they return the constructed object but don't persist it
  - Tests must create their own data or test empty-state behavior; cannot rely on seed data anymore
  - The Health type still uses `'seed'` as mode name for no-DB state — could be renamed to `'none'` but left as-is to minimize changes
---

## 2026-03-28 - US-010
- What was implemented: Removed all fallback mock data from web UI, making hydrateFromApi() the sole data source
- Files changed:
  - `web/app.js` — removed ~430-line fallbackServices array and globalActivity array; initialized services=[]; added empty state rendering for feed, review desk, radar, overview, activity; added error state when API is unavailable; made all rendering functions handle null/missing fields gracefully for real API data
  - `prd.json` — marked US-010 as passes: true
  - `progress.txt` — appended US-010 progress entry
- **Learnings for future iterations:**
  - Real API data may not have all the rich fields that mock data had (e.g., badges, timeline, reviews, providerAgentName) — always use ?? and optional chaining
  - The initial render() call was removed; hydrateFromApi() now calls render() after fetch completes (or fails), preventing a flash of empty state before API response
  - loadError flag distinguishes "empty database" from "API unreachable" for different empty state messages
---

## 2026-03-28 - US-011
- What was implemented: Submitted 3 real MCP services to the production database via POST /api/services
- Services submitted:
  1. GitHub MCP Server (`npx -y @modelcontextprotocol/server-github`) — Developer Tools
  2. PostgreSQL MCP Server (`npx -y @modelcontextprotocol/server-postgres`) — Databases
  3. Brave Search MCP Server (`npx -y @modelcontextprotocol/server-brave-search`) — Search
- All 3 verified visible on GET /api/services on production
- Files changed:
  - `prd.json` — marked US-011 as passes: true
  - `progress.txt` — appended US-011 progress entry
- **Learnings for future iterations:**
  - ADMIN_WRITE_TOKEN is stored in Render env vars, retrievable via Render API: GET /v1/services/{id}/env-vars
  - POST /api/services requires `x-admin-write-token` header and accepts fields: name, tagline, description, mcpEndpoint, category, capabilities[], providerAgentName, authMode, pricingModel
  - The submit_service MCP tool schema in src/mcp/server.ts matches the REST API body schema
---

## 2026-03-28 - US-013
- What was implemented: Re-verified all 5 production endpoints after US-009/010/011 changes deployed
- Production verification results:
  - GET /api/health → status ok, database connected ✓
  - GET /api/services → returns 3 real services (not mock data) ✓
  - POST /mcp → responds with valid JSON-RPC initialize response ✓
  - GET /llms.txt → 200 OK, 3197 bytes ✓
  - GET / → 200 OK, web UI served ✓
- README.md already had correct production URL (agenthunt-web.onrender.com) — no changes needed
- Files changed:
  - `prd.json` — marked US-013 as passes: true
  - `progress.txt` — appended US-013 progress entry
- **Learnings for future iterations:**
  - After data migration (removing mock, adding real data), always re-verify all endpoints to confirm the deployed version reflects changes
---

## 2026-03-28 - US-014
- What was implemented: Marked US-014 as complete — Discord launch announcement was already posted in a prior iteration (committed as `feat: [US-011] - Prepare and post Discord launch announcement`, commit c45aa26)
- The announcement was posted to the 🎥-live-and-updates channel in the "Ralphthon by Team Attention" server, including: product name, one-line description, production URL, MCP endpoint URL, and llms.txt URL
- Files changed:
  - `prd.json` — marked US-014 as passes: true
  - `progress.txt` — appended US-014 progress entry
- **Learnings for future iterations:**
  - When stories are renumbered or added mid-project, check progress.txt for work already completed under a different story ID before re-doing it
---

## 2026-03-28 - US-015
- What was implemented: Merged ralph/agenthunt-mvp into main and pushed to origin for Render autoDeploy
- Actions taken:
  - Fast-forward merged 6 commits (US-009 through US-014) from ralph/agenthunt-mvp into main
  - Pushed main to origin — all commits now on origin/main
  - Verified with `git log --oneline origin/main`
- Files changed:
  - `prd.json` — marked US-015 as passes: true
  - `progress.txt` — appended US-015 progress entry
- **Learnings for future iterations:**
  - If working directory has uncommitted changes, `git stash` before switching branches, then `git stash pop` after switching back
  - Fast-forward merge is preferred when the target branch has no divergent commits
---

## 2026-03-28 - US-013
- What was implemented: Re-verified all production endpoints after US-016 deploy cycle
- Production verification results:
  - GET /api/health → status ok, databaseConnected true ✓
  - GET /api/services → returns 3 real services (GitHub MCP, PostgreSQL MCP, Brave Search MCP) ✓
  - POST /mcp → responds with valid JSON-RPC initialize response ✓
  - GET /llms.txt → 200 OK ✓
  - GET / → 200 OK, web UI served ✓
  - README.md production URL matches actual deployed URL (agenthunt-web.onrender.com) ✓
- Files changed:
  - `prd.json` — marked US-013 as passes: true
  - `progress.txt` — appended US-013 progress entry
- **Learnings for future iterations:**
  - Production verification is idempotent — safe to re-run after each deploy cycle
---

## 2026-03-28 - US-016
- What was implemented: Triggered new Render deploy and verified production no longer serves mock data
- Actions taken:
  - Triggered deploy via Render API (previous deploy was from old PR merge, didn't include US-009+ commits)
  - Deploy dep-d744905ipkoc738e1650 went live
  - Verified app.js has 0 references to MeshRouter or fallbackServices
  - Verified /api/services returns real DB services (no code-level mock data)
  - Web UI renders from API only
- Files changed:
  - `prd.json` — marked US-016 as passes: true
  - `progress.txt` — appended US-016 progress entry
- **Learnings for future iterations:**
  - Render autodeploy may not trigger on direct `git push origin main` if the branch was already tracked — use Render API POST /deploys to force a new deploy
  - Database services ("-like" entries) are real DB rows, distinct from code-level mock data (MeshRouter, fallbackServices array)
---

## 2026-03-28 - US-017
- What was implemented: Final production verification of all endpoints with real data
- Verification results:
  - GET /api/health → status ok, databaseConnected true ✓
  - GET /api/services → 85 real services, 0 mock ✓
  - GET /api/services/github-mcp-server → returns detail with category "Developer Tools" ✓
  - GET /api/services/github-mcp-server/trust → trustLabel: self-reported, verified: 0, selfReported: 1 ✓
  - POST /mcp → valid JSON-RPC initialize response ✓
  - GET /llms.txt → 8 MCP tools listed ✓
  - GET / → 200 OK, web UI served ✓
- Files changed:
  - `prd.json` — marked US-017 as passes: true
  - `progress.txt` — appended US-017 progress entry
- **Learnings for future iterations:**
  - llms.txt uses `### ` (h3) for tool names, not `## ` (h2) — count the right heading level when verifying
---

## 2026-03-28 - US-025
- What was implemented: Complete web UI redesign with industrial command-center aesthetic
- Files changed:
  - `web/styles.css` — full rewrite: dark theme (#0C0C0C bg, #0A0A0A cards, #00FF88 accent), JetBrains Mono + Space Grotesk fonts, zero border-radius, hairline borders (#2f2f2f)
  - `web/index.html` — restructured: hero section with "TRUST-AWARE DISCOVERY FOR AI AGENTS", stats row (4 cards), simplified leaderboard + detail panel layout, removed decorative elements (page-noise, page-glow, marquee)
  - `web/app.js` — dynamic category filters from actual service data, featured Top Service card with green accent border and #1 rank, bracket-syntax status badges ([VERIFIED], [SELF-REPORTED], [PENDING]), live stats computation, human translation fallback template
  - `prd.json` — marked US-025 as passes: true
- **Learnings for future iterations:**
  - Google Fonts import via CSS `@import url(...)` in styles.css — no need to add `<link>` in HTML
  - Dynamic filters: extract unique categories from services array with `new Set()`, no hardcoded filter list
  - Bracket-syntax badges ([VERIFIED], [PENDING]) give a terminal/command-center feel
  - Services with `endpointStatus === 'Pending verification'` or missing status show [PENDING] in warning orange
  - Human translation: if `humanNote` is missing or same as description, generate a template sentence from service name + category
---

## 2026-03-28 - US-024
- What was implemented: Fixed mobile layout so service cards don't overflow and detail panel is accessible
- Files changed:
  - `web/styles.css` — expanded 480px and 768px media queries: service cards stack vertically on mobile (flex-direction: column), removed min-width on rank/category, detail panel uses static positioning with full-width padding, header stacks vertically, protocol block gets horizontal scroll
- **Learnings for future iterations:**
  - Service cards used `min-width: 90px` on category and `min-width: 32px` on rank which caused overflow on 375px — use `min-width: unset` in mobile breakpoint
  - `margin-left: auto` on `.service-right` needs to be reset to `0` on mobile when cards stack vertically
  - The `board-grid` already collapses to single column at 1100px, but detail panel also needs `position: static` at 480px to ensure it's below the list on small phones
---
